import {TouchableOpacity, Image} from 'react-native';
import styled from 'styled-components/native';

export const IconTouchable = styled(TouchableOpacity)`IconTouchable
`;

export const IconImage = styled(Image)`
  width: 24px;
  height: 24px;
`;
